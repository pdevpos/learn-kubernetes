apiVersion: v1
kind: Pod
metadata:
  name: pod-04po
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: nginx-path
          mountPath: /usr/share/nginx/html
    - name: nginx-content
      image: pavanidevops78/kubernetes:v2
      volumeMounts:
        - name: nginx-path
          mountPath: /usr/share/nginx/html
  volumes:
    - name: nginx-path
      emptyDir: {}