apiVersion: v1
kind: Pod
metadata:
  name: pod-04
spec:
  initContainers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: nginx-path
          mountPath: /usr/share/nginx/html
#  containers:
#    - name: nginx-content
#      image: pavanidevops78/kubernetes:v3
#      volumeMounts:
#        - name: nginx-path
#          mountPath: /usr/share/nginx/html

  volumes:
    - name: nginx-path
      emptyDir: {}